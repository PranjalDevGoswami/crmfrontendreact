import React, { useEffect, useState } from "react";

const DefaultDashboard = ({ showEdit }) => {
  return (
    <div className="mt-16">
      <h1 className="text-3xl">
        Please Connect with Admin for Role and Department Assign!!
      </h1>
    </div>
  );
};

export default DefaultDashboard;
